# Default LLM provider is Ollama, running on the host (not in this container).
OLLAMA_HOST=http://host.docker.internal:11434

# Only needed if you run with --llm-provider claude / --llm-provider openai
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
